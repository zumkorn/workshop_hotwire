<%= render partial: "header", locals: {artist:} %>
<%= turbo_frame_tag dom_id(artist, :content) do %>
  <div class="flex flex-row justify-between items-baseline">
    <h2 class="mt-4 text-header-2">Tracks</h2>
  </div>
  <div class="mt2">
    <%= render partial: "tracks_list", locals: {tracks:, artist:, has_next_page:} %>
  </div>
<% end %>
<hr class="my-4"/>
